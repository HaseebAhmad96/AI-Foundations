LangChain: Components, Models & Prompts

Task: 
● Dynamic Prompt Engine: Build a LangChain tool that accepts a topic and audience 
level, constructs a customised ChatPromptTemplate, sends it to two different model 
providers via their LangChain wrappers, and streams both responses to the terminal in 
real time with clear provider labels. 